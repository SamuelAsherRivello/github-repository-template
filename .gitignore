.setup/

# Node and Vite output.
node_modules/
/project-name/dist/

# Local configuration. Commit examples only, never credentials.
.env
.env.*
# Playwright CLI output (may contain credentials)
.playwright-cli/
